const express = require('express');
const router = express.Router();

// Aryan — functions 21-26
// GET    /api/service-requests         → list all active service requests
// POST   /api/service-requests         → create a new service request
// GET    /api/service-requests/:id     → get details for a specific request
// PATCH  /api/service-requests/:id/status → update request status

module.exports = router;