{
  "name": "parkly-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Express API for Parkly. Install deps: npm install",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "NODE_ENV=test jest src/authentication.test.js --forceExit --verbose"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "validator": "^13.15.35"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
